node_modules
.next
.next/
out
build
coverage
.vitest
*.tsbuildinfo
# Worker tsc output — built into Dockerfile.worker layer, never committed.
dist/
.env
.env.local
.env.*.local
.DS_Store
Thumbs.db
# Scratch files from local validation runs (e.g. tmp compose checks)
.tmp-*
*.tmp
# Common log droppings
*.log
npm-debug.log*
yarn-debug.log*
# Playwright
test-results/
playwright-report/
tests/e2e/.fixture.json

# Production secrets / Cloudflare Tunnel runtime artifacts
.env.prod
.env.prod.local
.env.prod.test
deploy/cloudflared/credentials.json
deploy/cloudflared/config.yml
deploy/cloudflared/cert.pem

# Per-boot dev SESSION_PASSWORD cache (generated by dev.bat)
.dev-session-password

# Per-machine imgproxy signing material (generated by dev.bat)
.dev-imgproxy-key
.dev-imgproxy-salt

# Local scratch directories from the Superpowers brainstorm/sketch tooling
.superpowers/
